{
    "required": false,
    "minVersion": "${mixin_min_version}",
    "package": "${maven_group}.${mod_id}.mixin",
    "compatibilityLevel": "JAVA_${java_version}",
    "mixins": [
    ],
    "client": [
    ],
    "injectors": {
        "defaultRequire": 1
    }
}